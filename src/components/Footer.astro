---
const base = import.meta.env.BASE_URL;
const currentYear = new Date().getFullYear();

const footerLinks = {
  navigate: [
    { href: `${base}/about`, label: 'About' },
    { href: `${base}/services`, label: 'Services' },
    { href: `${base}/case-studies`, label: 'Case Studies' },
    { href: `${base}/insights`, label: 'Insights' },
    { href: `${base}/contact`, label: 'Contact' },
  ],
  connect: [
    { href: 'https://www.linkedin.com/in/chandrimaganguly/', label: 'LinkedIn', external: true },
    { href: 'https://chandrimaganguly.substack.com', label: 'Substack', external: true },
  ],
};
---

<footer class="bg-forest-deep text-soft-cream">
  <!-- Main Footer -->
  <div class="container-wide mx-auto section-padding">
    <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-12">
      <!-- Brand Column -->
      <div class="lg:col-span-2">
        <a href={base} class="inline-block mb-4">
          <span class="font-heading text-2xl font-bold text-soft-cream">
            Chandrima Ganguly
          </span>
          <span class="block text-sm text-sage mt-1">Ph.D.</span>
        </a>
        <p class="text-soft-cream/80 max-w-md mb-6">
          Helping startups, social enterprises, and public sector organizations
          scale with AI — thoughtfully, sustainably, and in service of your mission.
        </p>
        <p class="font-accent text-xl text-morning-gold">
          Innovation with intention. Always.
        </p>
      </div>

      <!-- Navigate Column -->
      <div>
        <h4 class="font-heading text-lg font-semibold mb-4 text-soft-cream">Navigate</h4>
        <ul class="space-y-3">
          {footerLinks.navigate.map((link) => (
            <li>
              <a
                href={link.href}
                class="text-soft-cream/70 hover:text-morning-gold transition-colors duration-300"
              >
                {link.label}
              </a>
            </li>
          ))}
        </ul>
      </div>

      <!-- Connect Column -->
      <div>
        <h4 class="font-heading text-lg font-semibold mb-4 text-soft-cream">Connect</h4>
        <ul class="space-y-3">
          {footerLinks.connect.map((link) => (
            <li>
              <a
                href={link.href}
                target={link.external ? '_blank' : undefined}
                rel={link.external ? 'noopener noreferrer' : undefined}
                class="text-soft-cream/70 hover:text-morning-gold transition-colors duration-300 inline-flex items-center gap-1"
              >
                {link.label}
                {link.external && (
                  <svg class="w-3 h-3" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10 6H6a2 2 0 00-2 2v10a2 2 0 002 2h10a2 2 0 002-2v-4M14 4h6m0 0v6m0-6L10 14" />
                  </svg>
                )}
              </a>
            </li>
          ))}
          <li>
            <a
              href="mailto:hello@chandrimaganguly.com"
              class="text-soft-cream/70 hover:text-morning-gold transition-colors duration-300"
            >
              Email
            </a>
          </li>
        </ul>

        <!-- Newsletter CTA -->
        <div class="mt-8">
          <a
            href="https://chandrimaganguly.substack.com"
            target="_blank"
            rel="noopener noreferrer"
            class="inline-flex items-center gap-2 text-morning-gold hover:text-soft-cream transition-colors"
          >
            <svg class="w-5 h-5" fill="currentColor" viewBox="0 0 24 24">
              <path d="M22.539 8.242H1.46V5.406h21.08v2.836zM1.46 10.812V24L12 18.11 22.54 24V10.812H1.46zM22.54 0H1.46v2.836h21.08V0z"/>
            </svg>
            Subscribe to Newsletter
          </a>
        </div>
      </div>
    </div>
  </div>

  <!-- Bottom Bar -->
  <div class="border-t border-soft-cream/10">
    <div class="container-wide mx-auto px-6 md:px-8 py-6">
      <div class="flex flex-col md:flex-row justify-between items-center gap-4 text-sm text-soft-cream/60">
        <p>&copy; {currentYear} Chandrima Ganguly. All rights reserved.</p>
        <p class="flex items-center gap-1">
          Built with
          <svg class="w-4 h-4 text-terracotta" fill="currentColor" viewBox="0 0 24 24">
            <path d="M12 21.35l-1.45-1.32C5.4 15.36 2 12.28 2 8.5 2 5.42 4.42 3 7.5 3c1.74 0 3.41.81 4.5 2.09C13.09 3.81 14.76 3 16.5 3 19.58 3 22 5.42 22 8.5c0 3.78-3.4 6.86-8.55 11.54L12 21.35z"/>
          </svg>
          and intention
        </p>
      </div>
    </div>
  </div>
</footer>
